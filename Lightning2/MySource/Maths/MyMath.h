#pragma once

const float PI          = 3.14159265359f;
const float TWO_PI      = PI * 2.f;
const float PI_OVER_TWO = PI / 2.f;

inline float DegToRad(float angleDeg) { return angleDeg * (PI / 180.f); };